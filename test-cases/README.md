# AQMS Test Cases

This folder contains module-wise test cases aligned with the project report:

- `token-module-test-cases.md` (`TC-01` to `TC-05`)
- `yolo-detection-test-cases.md` (`TC-06` to `TC-10`)
- `forecasting-test-cases.md` (`TC-11` to `TC-14`)

Use these as:
- documentation test evidence for project review/demo
- a checklist during manual QA
- a base for future automated test scripts
